const DiyTitle = ({ text }: { text: string }) => {
  return (
    <h1 className="mt-0 text-primary -mx-0.5 break-words text-5xl font-display font-bold leading-tight">
      {text}
    </h1>
  );
};

export default DiyTitle;
